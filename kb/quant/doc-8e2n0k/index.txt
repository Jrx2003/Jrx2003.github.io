3:I[39275,[],""]
6:I[61343,[],""]
7:I[62782,["2015","static/chunks/2015-af9c16aac7ff4ad0.js","7138","static/chunks/7138-b897b2ad3516dd35.js","3185","static/chunks/app/layout-cd1f76c760ed59d2.js"],"Header"]
8:I[82446,["2015","static/chunks/2015-af9c16aac7ff4ad0.js","7138","static/chunks/7138-b897b2ad3516dd35.js","9160","static/chunks/app/not-found-b043129a42c3b49c.js"],"default"]
9:I[231,["2015","static/chunks/2015-af9c16aac7ff4ad0.js","7138","static/chunks/7138-b897b2ad3516dd35.js","3185","static/chunks/app/layout-cd1f76c760ed59d2.js"],""]
4:["category","quant","d"]
5:["slug","doc-8e2n0k","d"]
0:["ffk0ESDk0CTyiiGJbvRpT",[[["",{"children":["kb",{"children":[["category","quant","d"],{"children":[["slug","doc-8e2n0k","d"],{"children":["__PAGE__?{\"category\":\"quant\",\"slug\":\"doc-8e2n0k\"}",{}]}]}]}]},"$undefined","$undefined",true],["",{"children":["kb",{"children":[["category","quant","d"],{"children":[["slug","doc-8e2n0k","d"],{"children":["__PAGE__",{},[["$L1","$L2"],null],null]},["$","$L3",null,{"parallelRouterKey":"children","segmentPath":["children","kb","children","$4","children","$5","children"],"error":"$undefined","errorStyles":"$undefined","errorScripts":"$undefined","template":["$","$L6",null,{}],"templateStyles":"$undefined","templateScripts":"$undefined","notFound":"$undefined","notFoundStyles":"$undefined","styles":null}],null]},["$","$L3",null,{"parallelRouterKey":"children","segmentPath":["children","kb","children","$4","children"],"error":"$undefined","errorStyles":"$undefined","errorScripts":"$undefined","template":["$","$L6",null,{}],"templateStyles":"$undefined","templateScripts":"$undefined","notFound":"$undefined","notFoundStyles":"$undefined","styles":null}],null]},["$","$L3",null,{"parallelRouterKey":"children","segmentPath":["children","kb","children"],"error":"$undefined","errorStyles":"$undefined","errorScripts":"$undefined","template":["$","$L6",null,{}],"templateStyles":"$undefined","templateScripts":"$undefined","notFound":"$undefined","notFoundStyles":"$undefined","styles":null}],null]},[["$","html",null,{"lang":"zh-CN","className":"dark scroll-smooth","children":["$","body",null,{"className":"font-sans antialiased min-h-screen flex flex-col bg-background text-foreground","children":[["$","$L7",null,{}],["$","main",null,{"className":"flex-1","children":["$","$L3",null,{"parallelRouterKey":"children","segmentPath":["children"],"error":"$undefined","errorStyles":"$undefined","errorScripts":"$undefined","template":["$","$L6",null,{}],"templateStyles":"$undefined","templateScripts":"$undefined","notFound":["$","$L8",null,{}],"notFoundStyles":[],"styles":null}]}],["$","footer",null,{"className":"relative border-t border-white/5","children":[["$","div",null,{"className":"absolute inset-0 bg-gradient-to-t from-purple-500/5 to-transparent pointer-events-none"}],["$","div",null,{"className":"container relative py-16","children":[["$","div",null,{"className":"grid grid-cols-1 md:grid-cols-4 gap-12","children":[["$","div",null,{"className":"md:col-span-2 space-y-4","children":[["$","$L9",null,{"href":"/","className":"inline-block","children":["$","span",null,{"className":"text-2xl font-bold gradient-text","children":"Jerry Xu"}]}],["$","p",null,{"className":"text-muted-foreground max-w-sm","children":"热爱技术与生活的开发者。关注 AI Agent、Web 工具、三维图形与系统工程，同时享受音乐剧、F1 和游戏带来的乐趣。"}],["$","div",null,{"className":"flex items-center gap-4","children":["$","a",null,{"href":"https://github.com/Jrx2003","target":"_blank","rel":"noopener noreferrer","className":"flex items-center gap-1 text-sm text-muted-foreground hover:text-foreground transition-colors group","children":[["$","svg",null,{"xmlns":"http://www.w3.org/2000/svg","width":24,"height":24,"viewBox":"0 0 24 24","fill":"none","stroke":"currentColor","strokeWidth":2,"strokeLinecap":"round","strokeLinejoin":"round","className":"lucide lucide-github w-4 h-4","children":[["$","path","tonef",{"d":"M15 22v-4a4.8 4.8 0 0 0-1-3.5c3 0 6-2 6-5.5.08-1.25-.27-2.48-1-3.5.28-1.15.28-2.35 0-3.5 0 0-1 0-3 1.5-2.64-.5-5.36-.5-8 0C6 2 5 2 5 2c-.3 1.15-.3 2.35 0 3.5A5.403 5.403 0 0 0 4 9c0 3.5 3 5.5 6 5.5-.39.49-.68 1.05-.85 1.65-.17.6-.22 1.23-.15 1.85v4"}],["$","path","9comsn",{"d":"M9 18c-4.51 2-5-2-7-2"}],"$undefined"]}],["$","span",null,{"children":"GitHub"}],["$","svg",null,{"xmlns":"http://www.w3.org/2000/svg","width":24,"height":24,"viewBox":"0 0 24 24","fill":"none","stroke":"currentColor","strokeWidth":2,"strokeLinecap":"round","strokeLinejoin":"round","className":"lucide lucide-arrow-up-right w-3 h-3 opacity-0 -translate-y-1 translate-x-1 group-hover:opacity-100 group-hover:translate-y-0 group-hover:translate-x-0 transition-all","children":[["$","path","1tivn9",{"d":"M7 7h10v10"}],["$","path","1vkiza",{"d":"M7 17 17 7"}],"$undefined"]}]]}]}]]}],["$","div",null,{"children":[["$","h4",null,{"className":"font-semibold mb-4 text-sm uppercase tracking-wider text-muted-foreground","children":"导航"}],["$","ul",null,{"className":"space-y-3","children":[["$","li","/about/",{"children":["$","$L9",null,{"href":"/about/","className":"text-muted-foreground hover:text-foreground transition-colors text-sm","children":"关于"}]}],["$","li","/projects/",{"children":["$","$L9",null,{"href":"/projects/","className":"text-muted-foreground hover:text-foreground transition-colors text-sm","children":"项目"}]}],["$","li","/kb/",{"children":["$","$L9",null,{"href":"/kb/","className":"text-muted-foreground hover:text-foreground transition-colors text-sm","children":"知识库"}]}],["$","li","/blog/",{"children":["$","$L9",null,{"href":"/blog/","className":"text-muted-foreground hover:text-foreground transition-colors text-sm","children":"博客"}]}]]}]]}],["$","div",null,{"children":[["$","h4",null,{"className":"font-semibold mb-4 text-sm uppercase tracking-wider text-muted-foreground","children":"联系方式"}],["$","ul",null,{"className":"space-y-3 text-sm","children":[["$","li",null,{"children":["$","a",null,{"href":"https://www.sjtu.edu.cn","target":"_blank","rel":"noopener noreferrer","className":"text-muted-foreground hover:text-foreground transition-colors inline-flex items-center gap-1 group","children":["上海交通大学",["$","svg",null,{"xmlns":"http://www.w3.org/2000/svg","width":24,"height":24,"viewBox":"0 0 24 24","fill":"none","stroke":"currentColor","strokeWidth":2,"strokeLinecap":"round","strokeLinejoin":"round","className":"lucide lucide-arrow-up-right w-3 h-3 opacity-0 group-hover:opacity-100 transition-opacity","children":[["$","path","1tivn9",{"d":"M7 7h10v10"}],["$","path","1vkiza",{"d":"M7 17 17 7"}],"$undefined"]}]]}]}],["$","li",null,{"children":["$","a",null,{"href":"https://www.cuhk.edu.hk","target":"_blank","rel":"noopener noreferrer","className":"text-muted-foreground hover:text-foreground transition-colors inline-flex items-center gap-1 group","children":["即将前往香港中文大学",["$","svg",null,{"xmlns":"http://www.w3.org/2000/svg","width":24,"height":24,"viewBox":"0 0 24 24","fill":"none","stroke":"currentColor","strokeWidth":2,"strokeLinecap":"round","strokeLinejoin":"round","className":"lucide lucide-arrow-up-right w-3 h-3 opacity-0 group-hover:opacity-100 transition-opacity","children":[["$","path","1tivn9",{"d":"M7 7h10v10"}],["$","path","1vkiza",{"d":"M7 17 17 7"}],"$undefined"]}]]}]}],["$","li",null,{"className":"text-xs pt-2 text-muted-foreground","children":"通过 GitHub 联系"}]]}]]}]]}],["$","div",null,{"className":"mt-16 pt-8 border-t border-white/5 flex flex-col sm:flex-row items-center justify-between gap-4","children":[["$","p",null,{"className":"text-sm text-muted-foreground","children":["© ",2026," Jerry Xu. Built with"," ",["$","svg",null,{"xmlns":"http://www.w3.org/2000/svg","width":24,"height":24,"viewBox":"0 0 24 24","fill":"none","stroke":"currentColor","strokeWidth":2,"strokeLinecap":"round","strokeLinejoin":"round","className":"lucide lucide-heart inline w-3 h-3 text-red-500 fill-red-500","children":[["$","path","c3ymky",{"d":"M19 14c1.49-1.46 3-3.21 3-5.5A5.5 5.5 0 0 0 16.5 3c-1.76 0-3 .5-4.5 2-1.5-1.5-2.74-2-4.5-2A5.5 5.5 0 0 0 2 8.5c0 2.3 1.5 4.05 3 5.5l7 7Z"}],"$undefined"]}]," and Next.js"]}],["$","p",null,{"className":"text-xs text-muted-foreground/60","children":"Designed & Developed by Jerry"}]]}]]}]]}]]}]}],null],null],[[["$","link","0",{"rel":"stylesheet","href":"/_next/static/css/8fd3907a9da855dd.css","precedence":"next","crossOrigin":"$undefined"}]],"$La"]]]]
b:I[66006,["2015","static/chunks/2015-af9c16aac7ff4ad0.js","7138","static/chunks/7138-b897b2ad3516dd35.js","8373","static/chunks/8373-886bc01b77cafd14.js","7438","static/chunks/app/kb/%5Bcategory%5D/%5Bslug%5D/page-1c0350979f9bea88.js"],"default"]
c:T3e0a,# 配对交易策略实战

> 统计套利的经典应用——寻找"分久必合"的价格关系

---

## 策略概述

### 什么是配对交易？

**核心思想**：找到两只价格走势高度相关的股票，当它们的价格关系偏离历史正常水平时，做多被低估的，做空被高估的，等待价格关系回归时平仓获利。

### 直观理解

想象两个人用绳子绑在一起走路：
- **正常情况**：两人保持固定距离前进
- **距离拉大**：一个人走太快，另一个人被拉住
- **距离缩小**：一个人走太慢，另一个人推着他
- **最终状态**：绳子将他们拉回正常距离

**配对交易**就是赌"绳子"最终起作用。

### 策略要素

| 要素 | 说明 |
|------|------|
| **配对选择** | 找到价格相关的两只股票 |
| **价差计算** | 量化两只股票的价格关系 |
| **信号生成** | 判断何时偏离、何时回归 |
| **仓位管理** | 确定多空比例 |
| **止损机制** | 防止"绳子断裂" |

---

## 理论基础

### 协整（Cointegration）

**定义**：两个非平稳序列存在长期均衡关系。

**vs 相关性的区别**：
- **相关性**：同时变化（可能随机）
- **协整**：长期不会偏离（有"绳子"拉着）

**检验方法**：Engle-Granger两步法

```python
from statsmodels.tsa.stattools import coint, adfuller
import pandas as pd
import numpy as np

def check_cointegration(price1, price2):
    """
    检验两只股票是否协整
    """
    # 协整检验
    score, pvalue, _ = coint(price1, price2)

    print(f"协整检验统计量: {score:.4f}")
    print(f"P值: {pvalue:.4f}")

    if pvalue < 0.05:
        print("✓ 两只股票协整（存在长期均衡关系）")
        is_cointegrated = True
    else:
        print("✗ 两只股票不协整")
        is_cointegrated = False

    # 计算对冲比率（OLS回归）
    from sklearn.linear_model import LinearRegression

    X = price2.values.reshape(-1, 1)
    y = price1.values

    model = LinearRegression().fit(X, y)
    hedge_ratio = model.coef_[0]

    print(f"\n对冲比率: {hedge_ratio:.4f}")
    print(f"即：1股股票1 ≈ {hedge_ratio:.4f}股股票2")

    # 检验价差的平稳性
    spread = price1 - hedge_ratio * price2
    adf_result = adfuller(spread)

    print(f"\n价差ADF检验P值: {adf_result[1]:.4f}")
    if adf_result[1] < 0.05:
        print("✓ 价差是平稳的（适合配对交易）")
    else:
        print("✗ 价差不平稳（风险较高）")

    return is_cointegrated, hedge_ratio, spread

# 示例：生成协整数据
np.random.seed(42)
n = 500

# 股票2的随机游走
price2 = 100 + np.cumsum(np.random.randn(n) * 0.5)

# 股票1与股票2协整
beta = 0.7
spread_stationary = np.random.randn(n).cumsum() * 0.1
price1 = 50 + beta * (price2 - 100) + spread_stationary

# 检验
check_cointegration(pd.Series(price1), pd.Series(price2))
```

### 价差（Spread）

**计算**：
```
价差 = 价格1 - 对冲比率 × 价格2
```

**标准化（Z-Score）**：
```
Z = (价差 - 均值) / 标准差
```

---

## 完整策略实现

```python
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
from sklearn.linear_model import LinearRegression
from statsmodels.tsa.stattools import coint

plt.rcParams['font.sans-serif'] = ['SimHei', 'DejaVu Sans']
plt.rcParams['axes.unicode_minus'] = False


class PairTradingStrategy:
    """
    配对交易策略
    """

    def __init__(self, lookback=60, entry_z=2.0, exit_z=0.5, stop_loss=3.0):
        """
        初始化策略

        Parameters:
        -----------
        lookback : int
            计算价差的回看周期
        entry_z : float
            入场Z-Score阈值
        exit_z : float
            出场Z-Score阈值
        stop_loss : float
            止损Z-Score阈值
        """
        self.lookback = lookback
        self.entry_z = entry_z
        self.exit_z = exit_z
        self.stop_loss = stop_loss

    def calculate_hedge_ratio(self, price1, price2):
        """
        计算对冲比率
        """
        X = price2.values.reshape(-1, 1)
        y = price1.values

        model = LinearRegression().fit(X, y)
        return model.coef_[0]

    def calculate_spread(self, price1, price2, hedge_ratio):
        """
        计算价差
        """
        return price1 - hedge_ratio * price2

    def calculate_zscore(self, spread):
        """
        计算Z-Score（滚动窗口）
        """
        mean = spread.rolling(window=self.lookback).mean()
        std = spread.rolling(window=self.lookback).std()
        zscore = (spread - mean) / std

        return zscore

    def generate_signals(self, price1, price2):
        """
        生成交易信号

        Returns:
        --------
        signals : DataFrame
            包含价格和信号的DataFrame
        """
        # 计算对冲比率（使用历史数据，避免前视偏差）
        hedge_ratio = self.calculate_hedge_ratio(price1[:252], price2[:252])

        # 计算价差
        spread = self.calculate_spread(price1, price2, hedge_ratio)

        # 计算Z-Score
        zscore = self.calculate_zscore(spread)

        # 生成信号
        signals = pd.DataFrame(index=price1.index)
        signals['price1'] = price1
        signals['price2'] = price2
        signals['spread'] = spread
        signals['zscore'] = zscore
        signals['position'] = 0  # 0: 空仓, 1: 做多价差, -1: 做空价差

        position = 0
        entry_zscore = None

        for i in range(self.lookback, len(signals)):
            z = signals['zscore'].iloc[i]

            if position == 0:  # 空仓
                if z < -self.entry_z:  # 价差过低，做多价差
                    position = 1
                    entry_zscore = z
                elif z > self.entry_z:  # 价差过高，做空价差
                    position = -1
                    entry_zscore = z

            elif position == 1:  # 做多价差
                # 止盈或止损
                if z > -self.exit_z or z < -self.stop_loss:
                    position = 0
                    entry_zscore = None

            elif position == -1:  # 做空价差
                # 止盈或止损
                if z < self.exit_z or z > self.stop_loss:
                    position = 0
                    entry_zscore = None

            signals['position'].iloc[i] = position

        return signals, hedge_ratio

    def backtest(self, price1, price2, initial_capital=100000):
        """
        回测策略
        """
        signals, hedge_ratio = self.generate_signals(price1, price2)

        # 计算每日收益
        returns1 = price1.pct_change()
        returns2 = price2.pct_change()

        # 组合收益（多空组合）
        # position=1: 做多spread = 做多price1, 做空price2
        # position=-1: 做空spread = 做空price1, 做多price2
        portfolio_returns = pd.Series(0, index=signals.index)

        portfolio_returns[signals['position'] == 1] = (
            returns1[signals['position'] == 1] -
            hedge_ratio * returns2[signals['position'] == 1]
        )

        portfolio_returns[signals['position'] == -1] = (
            -returns1[signals['position'] == -1] +
            hedge_ratio * returns2[signals['position'] == -1]
        )

        # 计算组合价值
        portfolio = pd.DataFrame(index=signals.index)
        portfolio['returns'] = portfolio_returns
        portfolio['cumulative_returns'] = (1 + portfolio_returns).cumprod()
        portfolio['total_value'] = initial_capital * portfolio['cumulative_returns']

        return portfolio, signals

    def plot_results(self, price1, price2, signals, portfolio):
        """
        绘制回测结果
        """
        fig, axes = plt.subplots(4, 1, figsize=(14, 16))

        # 1. 价格走势
        ax1 = axes[0]
        ax1.plot(price1.index, price1 / price1.iloc[0], label='Stock 1', linewidth=2)
        ax1.plot(price2.index, price2 / price2.iloc[0], label='Stock 2', linewidth=2)
        ax1.set_title('Normalized Price Series')
        ax1.legend()
        ax1.grid(True, alpha=0.3)

        # 2. 价差
        ax2 = axes[1]
        ax2.plot(signals.index, signals['spread'], color='purple', label='Spread')
        ax2.axhline(signals['spread'].mean(), color='red', linestyle='--', alpha=0.5)
        ax2.set_title('Price Spread')
        ax2.legend()
        ax2.grid(True, alpha=0.3)

        # 3. Z-Score和信号
        ax3 = axes[2]
        ax3.plot(signals.index, signals['zscore'], color='orange', label='Z-Score')
        ax3.axhline(self.entry_z, color='red', linestyle='--', alpha=0.5, label='Entry')
        ax3.axhline(-self.entry_z, color='green', linestyle='--', alpha=0.5)
        ax3.axhline(self.exit_z, color='red', linestyle=':', alpha=0.3)
        ax3.axhline(-self.exit_z, color='green', linestyle=':', alpha=0.3)
        ax3.axhline(0, color='black', linestyle='-', alpha=0.3)

        # 标记持仓
        long_periods = signals[signals['position'] == 1].index
        short_periods = signals[signals['position'] == -1].index
        ax3.scatter(long_periods, signals.loc[long_periods, 'zscore'],
                   color='green', marker='^', s=30, alpha=0.7, label='Long Spread')
        ax3.scatter(short_periods, signals.loc[short_periods, 'zscore'],
                   color='red', marker='v', s=30, alpha=0.7, label='Short Spread')

        ax3.set_title('Z-Score and Trading Signals')
        ax3.legend()
        ax3.grid(True, alpha=0.3)

        # 4. 组合收益
        ax4 = axes[3]
        ax4.plot(portfolio.index, portfolio['total_value'], linewidth=2, label='Portfolio Value')
        ax4.axhline(portfolio['total_value'].iloc[0], color='gray', linestyle='--', alpha=0.5)
        ax4.set_title('Portfolio Value')
        ax4.legend()
        ax4.grid(True, alpha=0.3)

        plt.tight_layout()
        plt.show()


# ============== 实战示例 ==============

# 生成模拟数据（两只协整的股票）
np.random.seed(42)
dates = pd.date_range('2020-01-01', '2023-12-31', freq='B')
n = len(dates)

# 股票2：随机游走
returns2 = np.random.normal(0.0002, 0.015, n)
price2 = pd.Series(100 * np.exp(np.cumsum(returns2)), index=dates)

# 股票1：与股票2协整，但有均值回归的spread
beta = 0.7
spread_noise = np.random.normal(0, 1, n).cumsum() * 0.05  # 均值回归成分
price1 = pd.Series(50 + beta * (price2 - 100) + spread_noise, index=dates)

# 创建策略
strategy = PairTradingStrategy(lookback=60, entry_z=2.0, exit_z=0.5, stop_loss=3.0)

# 回测
portfolio, signals = strategy.backtest(price1, price2, initial_capital=100000)

# 绘制结果
strategy.plot_results(price1, price2, signals, portfolio)

# 计算绩效指标
total_return = (portfolio['total_value'].iloc[-1] / portfolio['total_value'].iloc[0] - 1) * 100
annual_return = ((1 + total_return/100) ** (252/len(portfolio)) - 1) * 100
volatility = portfolio['returns'].std() * np.sqrt(252) * 100
sharpe = annual_return / volatility if volatility > 0 else 0

# 最大回撤
cumulative = portfolio['total_value']
running_max = cumulative.expanding().max()
drawdown = (cumulative - running_max) / running_max
max_drawdown = drawdown.min() * 100

# 交易统计
position_changes = signals['position'].diff().abs()
n_trades = position_changes[position_changes > 0].count() / 2  # 开平算一次

print("=" * 50)
print("配对交易策略回测结果")
print("=" * 50)
print(f"参数: 回看={strategy.lookback}, 入场Z={strategy.entry_z}, "
      f"出场Z={strategy.exit_z}, 止损Z={strategy.stop_loss}")
print("-" * 50)
print(f"总收益率: {total_return:.2f}%")
print(f"年化收益率: {annual_return:.2f}%")
print(f"年化波动率: {volatility:.2f}%")
print(f"夏普比率: {sharpe:.2f}")
print(f"最大回撤: {max_drawdown:.2f}%")
print(f"交易次数: {int(n_trades)}")
print("=" * 50)
```

---

## 配对选择方法

### 1. 同一行业配对

```python
def find_pairs_same_industry(stock_data, industry_col='industry', min_correlation=0.8):
    """
    在同一行业内寻找配对
    """
    pairs = []

    for industry in stock_data[industry_col].unique():
        industry_stocks = stock_data[stock_data[industry_col] == industry]

        if len(industry_stocks) < 2:
            continue

        # 计算相关系数
        returns = industry_stocks.pct_change().dropna()
        corr_matrix = returns.corr()

        # 找高相关性的配对
        for i in range(len(corr_matrix.columns)):
            for j in range(i+1, len(corr_matrix.columns)):
                if corr_matrix.iloc[i, j] > min_correlation:
                    stock1 = corr_matrix.columns[i]
                    stock2 = corr_matrix.columns[j]

                    # 检验协整
                    _, pvalue, _ = coint(stock_data[stock1], stock_data[stock2])

                    if pvalue < 0.05:
                        pairs.append({
                            'stock1': stock1,
                            'stock2': stock2,
                            'industry': industry,
                            'correlation': corr_matrix.iloc[i, j],
                            'coint_pvalue': pvalue
                        })

    return pd.DataFrame(pairs).sort_values('coint_pvalue')
```

### 2. 统计筛选

```python
def scan_pairs(stock_prices, min_correlation=0.9, min_history=252):
    """
    扫描所有可能的配对
    """
    pairs = []
    stocks = stock_prices.columns

    for i in range(len(stocks)):
        for j in range(i+1, len(stocks)):
            stock1, stock2 = stocks[i], stocks[j]

            price1 = stock_prices[stock1].dropna()
            price2 = stock_prices[stock2].dropna()

            if len(price1) < min_history or len(price2) < min_history:
                continue

            # 对齐数据
            common_dates = price1.index.intersection(price2.index)
            p1, p2 = price1[common_dates], price2[common_dates]

            # 计算相关性
            returns1 = p1.pct_change().dropna()
            returns2 = p2.pct_change().dropna()
            correlation = returns1.corr(returns2)

            if correlation > min_correlation:
                # 检验协整
                _, pvalue, _ = coint(p1, p2)

                if pvalue < 0.05:
                    pairs.append({
                        'stock1': stock1,
                        'stock2': stock2,
                        'correlation': correlation,
                        'coint_pvalue': pvalue,
                        'n_observations': len(p1)
                    })

    return pd.DataFrame(pairs).sort_values('coint_pvalue')
```

---

## 风险管理

### 1. 协整破裂检测

```python
def detect_cointegration_breakdown(spread, lookback=60, threshold=3.0):
    """
    检测协整关系是否破裂
    """
    rolling_mean = spread.rolling(lookback).mean()
    rolling_std = spread.rolling(lookback).std()

    zscore = (spread - rolling_mean) / rolling_std

    # 如果Z-Score持续超出阈值，可能协整破裂
    breakdown = (zscore.abs() > threshold).rolling(20).sum() > 15

    return breakdown
```

### 2. 动态对冲比率

```python
def dynamic_hedge_ratio(price1, price2, window=60):
    """
    动态计算对冲比率
    """
    hedge_ratios = pd.Series(index=price1.index)

    for i in range(window, len(price1)):
        X = price2.iloc[i-window:i].values.reshape(-1, 1)
        y = price1.iloc[i-window:i].values

        model = LinearRegression().fit(X, y)
        hedge_ratios.iloc[i] = model.coef_[0]

    return hedge_ratios
```

---

## 下一步

- [均值回归策略](/kb/quant/quant-l6sjq7) - 深入学习均值回归
- [风险模型](/kb/quant/quant-s9qgsc) - 配对交易的风险管理
- [时间序列分析基础](/kb/quant/quant-bucbbs) - 统计基础
d:T3e0a,# 配对交易策略实战

> 统计套利的经典应用——寻找"分久必合"的价格关系

---

## 策略概述

### 什么是配对交易？

**核心思想**：找到两只价格走势高度相关的股票，当它们的价格关系偏离历史正常水平时，做多被低估的，做空被高估的，等待价格关系回归时平仓获利。

### 直观理解

想象两个人用绳子绑在一起走路：
- **正常情况**：两人保持固定距离前进
- **距离拉大**：一个人走太快，另一个人被拉住
- **距离缩小**：一个人走太慢，另一个人推着他
- **最终状态**：绳子将他们拉回正常距离

**配对交易**就是赌"绳子"最终起作用。

### 策略要素

| 要素 | 说明 |
|------|------|
| **配对选择** | 找到价格相关的两只股票 |
| **价差计算** | 量化两只股票的价格关系 |
| **信号生成** | 判断何时偏离、何时回归 |
| **仓位管理** | 确定多空比例 |
| **止损机制** | 防止"绳子断裂" |

---

## 理论基础

### 协整（Cointegration）

**定义**：两个非平稳序列存在长期均衡关系。

**vs 相关性的区别**：
- **相关性**：同时变化（可能随机）
- **协整**：长期不会偏离（有"绳子"拉着）

**检验方法**：Engle-Granger两步法

```python
from statsmodels.tsa.stattools import coint, adfuller
import pandas as pd
import numpy as np

def check_cointegration(price1, price2):
    """
    检验两只股票是否协整
    """
    # 协整检验
    score, pvalue, _ = coint(price1, price2)

    print(f"协整检验统计量: {score:.4f}")
    print(f"P值: {pvalue:.4f}")

    if pvalue < 0.05:
        print("✓ 两只股票协整（存在长期均衡关系）")
        is_cointegrated = True
    else:
        print("✗ 两只股票不协整")
        is_cointegrated = False

    # 计算对冲比率（OLS回归）
    from sklearn.linear_model import LinearRegression

    X = price2.values.reshape(-1, 1)
    y = price1.values

    model = LinearRegression().fit(X, y)
    hedge_ratio = model.coef_[0]

    print(f"\n对冲比率: {hedge_ratio:.4f}")
    print(f"即：1股股票1 ≈ {hedge_ratio:.4f}股股票2")

    # 检验价差的平稳性
    spread = price1 - hedge_ratio * price2
    adf_result = adfuller(spread)

    print(f"\n价差ADF检验P值: {adf_result[1]:.4f}")
    if adf_result[1] < 0.05:
        print("✓ 价差是平稳的（适合配对交易）")
    else:
        print("✗ 价差不平稳（风险较高）")

    return is_cointegrated, hedge_ratio, spread

# 示例：生成协整数据
np.random.seed(42)
n = 500

# 股票2的随机游走
price2 = 100 + np.cumsum(np.random.randn(n) * 0.5)

# 股票1与股票2协整
beta = 0.7
spread_stationary = np.random.randn(n).cumsum() * 0.1
price1 = 50 + beta * (price2 - 100) + spread_stationary

# 检验
check_cointegration(pd.Series(price1), pd.Series(price2))
```

### 价差（Spread）

**计算**：
```
价差 = 价格1 - 对冲比率 × 价格2
```

**标准化（Z-Score）**：
```
Z = (价差 - 均值) / 标准差
```

---

## 完整策略实现

```python
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
from sklearn.linear_model import LinearRegression
from statsmodels.tsa.stattools import coint

plt.rcParams['font.sans-serif'] = ['SimHei', 'DejaVu Sans']
plt.rcParams['axes.unicode_minus'] = False


class PairTradingStrategy:
    """
    配对交易策略
    """

    def __init__(self, lookback=60, entry_z=2.0, exit_z=0.5, stop_loss=3.0):
        """
        初始化策略

        Parameters:
        -----------
        lookback : int
            计算价差的回看周期
        entry_z : float
            入场Z-Score阈值
        exit_z : float
            出场Z-Score阈值
        stop_loss : float
            止损Z-Score阈值
        """
        self.lookback = lookback
        self.entry_z = entry_z
        self.exit_z = exit_z
        self.stop_loss = stop_loss

    def calculate_hedge_ratio(self, price1, price2):
        """
        计算对冲比率
        """
        X = price2.values.reshape(-1, 1)
        y = price1.values

        model = LinearRegression().fit(X, y)
        return model.coef_[0]

    def calculate_spread(self, price1, price2, hedge_ratio):
        """
        计算价差
        """
        return price1 - hedge_ratio * price2

    def calculate_zscore(self, spread):
        """
        计算Z-Score（滚动窗口）
        """
        mean = spread.rolling(window=self.lookback).mean()
        std = spread.rolling(window=self.lookback).std()
        zscore = (spread - mean) / std

        return zscore

    def generate_signals(self, price1, price2):
        """
        生成交易信号

        Returns:
        --------
        signals : DataFrame
            包含价格和信号的DataFrame
        """
        # 计算对冲比率（使用历史数据，避免前视偏差）
        hedge_ratio = self.calculate_hedge_ratio(price1[:252], price2[:252])

        # 计算价差
        spread = self.calculate_spread(price1, price2, hedge_ratio)

        # 计算Z-Score
        zscore = self.calculate_zscore(spread)

        # 生成信号
        signals = pd.DataFrame(index=price1.index)
        signals['price1'] = price1
        signals['price2'] = price2
        signals['spread'] = spread
        signals['zscore'] = zscore
        signals['position'] = 0  # 0: 空仓, 1: 做多价差, -1: 做空价差

        position = 0
        entry_zscore = None

        for i in range(self.lookback, len(signals)):
            z = signals['zscore'].iloc[i]

            if position == 0:  # 空仓
                if z < -self.entry_z:  # 价差过低，做多价差
                    position = 1
                    entry_zscore = z
                elif z > self.entry_z:  # 价差过高，做空价差
                    position = -1
                    entry_zscore = z

            elif position == 1:  # 做多价差
                # 止盈或止损
                if z > -self.exit_z or z < -self.stop_loss:
                    position = 0
                    entry_zscore = None

            elif position == -1:  # 做空价差
                # 止盈或止损
                if z < self.exit_z or z > self.stop_loss:
                    position = 0
                    entry_zscore = None

            signals['position'].iloc[i] = position

        return signals, hedge_ratio

    def backtest(self, price1, price2, initial_capital=100000):
        """
        回测策略
        """
        signals, hedge_ratio = self.generate_signals(price1, price2)

        # 计算每日收益
        returns1 = price1.pct_change()
        returns2 = price2.pct_change()

        # 组合收益（多空组合）
        # position=1: 做多spread = 做多price1, 做空price2
        # position=-1: 做空spread = 做空price1, 做多price2
        portfolio_returns = pd.Series(0, index=signals.index)

        portfolio_returns[signals['position'] == 1] = (
            returns1[signals['position'] == 1] -
            hedge_ratio * returns2[signals['position'] == 1]
        )

        portfolio_returns[signals['position'] == -1] = (
            -returns1[signals['position'] == -1] +
            hedge_ratio * returns2[signals['position'] == -1]
        )

        # 计算组合价值
        portfolio = pd.DataFrame(index=signals.index)
        portfolio['returns'] = portfolio_returns
        portfolio['cumulative_returns'] = (1 + portfolio_returns).cumprod()
        portfolio['total_value'] = initial_capital * portfolio['cumulative_returns']

        return portfolio, signals

    def plot_results(self, price1, price2, signals, portfolio):
        """
        绘制回测结果
        """
        fig, axes = plt.subplots(4, 1, figsize=(14, 16))

        # 1. 价格走势
        ax1 = axes[0]
        ax1.plot(price1.index, price1 / price1.iloc[0], label='Stock 1', linewidth=2)
        ax1.plot(price2.index, price2 / price2.iloc[0], label='Stock 2', linewidth=2)
        ax1.set_title('Normalized Price Series')
        ax1.legend()
        ax1.grid(True, alpha=0.3)

        # 2. 价差
        ax2 = axes[1]
        ax2.plot(signals.index, signals['spread'], color='purple', label='Spread')
        ax2.axhline(signals['spread'].mean(), color='red', linestyle='--', alpha=0.5)
        ax2.set_title('Price Spread')
        ax2.legend()
        ax2.grid(True, alpha=0.3)

        # 3. Z-Score和信号
        ax3 = axes[2]
        ax3.plot(signals.index, signals['zscore'], color='orange', label='Z-Score')
        ax3.axhline(self.entry_z, color='red', linestyle='--', alpha=0.5, label='Entry')
        ax3.axhline(-self.entry_z, color='green', linestyle='--', alpha=0.5)
        ax3.axhline(self.exit_z, color='red', linestyle=':', alpha=0.3)
        ax3.axhline(-self.exit_z, color='green', linestyle=':', alpha=0.3)
        ax3.axhline(0, color='black', linestyle='-', alpha=0.3)

        # 标记持仓
        long_periods = signals[signals['position'] == 1].index
        short_periods = signals[signals['position'] == -1].index
        ax3.scatter(long_periods, signals.loc[long_periods, 'zscore'],
                   color='green', marker='^', s=30, alpha=0.7, label='Long Spread')
        ax3.scatter(short_periods, signals.loc[short_periods, 'zscore'],
                   color='red', marker='v', s=30, alpha=0.7, label='Short Spread')

        ax3.set_title('Z-Score and Trading Signals')
        ax3.legend()
        ax3.grid(True, alpha=0.3)

        # 4. 组合收益
        ax4 = axes[3]
        ax4.plot(portfolio.index, portfolio['total_value'], linewidth=2, label='Portfolio Value')
        ax4.axhline(portfolio['total_value'].iloc[0], color='gray', linestyle='--', alpha=0.5)
        ax4.set_title('Portfolio Value')
        ax4.legend()
        ax4.grid(True, alpha=0.3)

        plt.tight_layout()
        plt.show()


# ============== 实战示例 ==============

# 生成模拟数据（两只协整的股票）
np.random.seed(42)
dates = pd.date_range('2020-01-01', '2023-12-31', freq='B')
n = len(dates)

# 股票2：随机游走
returns2 = np.random.normal(0.0002, 0.015, n)
price2 = pd.Series(100 * np.exp(np.cumsum(returns2)), index=dates)

# 股票1：与股票2协整，但有均值回归的spread
beta = 0.7
spread_noise = np.random.normal(0, 1, n).cumsum() * 0.05  # 均值回归成分
price1 = pd.Series(50 + beta * (price2 - 100) + spread_noise, index=dates)

# 创建策略
strategy = PairTradingStrategy(lookback=60, entry_z=2.0, exit_z=0.5, stop_loss=3.0)

# 回测
portfolio, signals = strategy.backtest(price1, price2, initial_capital=100000)

# 绘制结果
strategy.plot_results(price1, price2, signals, portfolio)

# 计算绩效指标
total_return = (portfolio['total_value'].iloc[-1] / portfolio['total_value'].iloc[0] - 1) * 100
annual_return = ((1 + total_return/100) ** (252/len(portfolio)) - 1) * 100
volatility = portfolio['returns'].std() * np.sqrt(252) * 100
sharpe = annual_return / volatility if volatility > 0 else 0

# 最大回撤
cumulative = portfolio['total_value']
running_max = cumulative.expanding().max()
drawdown = (cumulative - running_max) / running_max
max_drawdown = drawdown.min() * 100

# 交易统计
position_changes = signals['position'].diff().abs()
n_trades = position_changes[position_changes > 0].count() / 2  # 开平算一次

print("=" * 50)
print("配对交易策略回测结果")
print("=" * 50)
print(f"参数: 回看={strategy.lookback}, 入场Z={strategy.entry_z}, "
      f"出场Z={strategy.exit_z}, 止损Z={strategy.stop_loss}")
print("-" * 50)
print(f"总收益率: {total_return:.2f}%")
print(f"年化收益率: {annual_return:.2f}%")
print(f"年化波动率: {volatility:.2f}%")
print(f"夏普比率: {sharpe:.2f}")
print(f"最大回撤: {max_drawdown:.2f}%")
print(f"交易次数: {int(n_trades)}")
print("=" * 50)
```

---

## 配对选择方法

### 1. 同一行业配对

```python
def find_pairs_same_industry(stock_data, industry_col='industry', min_correlation=0.8):
    """
    在同一行业内寻找配对
    """
    pairs = []

    for industry in stock_data[industry_col].unique():
        industry_stocks = stock_data[stock_data[industry_col] == industry]

        if len(industry_stocks) < 2:
            continue

        # 计算相关系数
        returns = industry_stocks.pct_change().dropna()
        corr_matrix = returns.corr()

        # 找高相关性的配对
        for i in range(len(corr_matrix.columns)):
            for j in range(i+1, len(corr_matrix.columns)):
                if corr_matrix.iloc[i, j] > min_correlation:
                    stock1 = corr_matrix.columns[i]
                    stock2 = corr_matrix.columns[j]

                    # 检验协整
                    _, pvalue, _ = coint(stock_data[stock1], stock_data[stock2])

                    if pvalue < 0.05:
                        pairs.append({
                            'stock1': stock1,
                            'stock2': stock2,
                            'industry': industry,
                            'correlation': corr_matrix.iloc[i, j],
                            'coint_pvalue': pvalue
                        })

    return pd.DataFrame(pairs).sort_values('coint_pvalue')
```

### 2. 统计筛选

```python
def scan_pairs(stock_prices, min_correlation=0.9, min_history=252):
    """
    扫描所有可能的配对
    """
    pairs = []
    stocks = stock_prices.columns

    for i in range(len(stocks)):
        for j in range(i+1, len(stocks)):
            stock1, stock2 = stocks[i], stocks[j]

            price1 = stock_prices[stock1].dropna()
            price2 = stock_prices[stock2].dropna()

            if len(price1) < min_history or len(price2) < min_history:
                continue

            # 对齐数据
            common_dates = price1.index.intersection(price2.index)
            p1, p2 = price1[common_dates], price2[common_dates]

            # 计算相关性
            returns1 = p1.pct_change().dropna()
            returns2 = p2.pct_change().dropna()
            correlation = returns1.corr(returns2)

            if correlation > min_correlation:
                # 检验协整
                _, pvalue, _ = coint(p1, p2)

                if pvalue < 0.05:
                    pairs.append({
                        'stock1': stock1,
                        'stock2': stock2,
                        'correlation': correlation,
                        'coint_pvalue': pvalue,
                        'n_observations': len(p1)
                    })

    return pd.DataFrame(pairs).sort_values('coint_pvalue')
```

---

## 风险管理

### 1. 协整破裂检测

```python
def detect_cointegration_breakdown(spread, lookback=60, threshold=3.0):
    """
    检测协整关系是否破裂
    """
    rolling_mean = spread.rolling(lookback).mean()
    rolling_std = spread.rolling(lookback).std()

    zscore = (spread - rolling_mean) / rolling_std

    # 如果Z-Score持续超出阈值，可能协整破裂
    breakdown = (zscore.abs() > threshold).rolling(20).sum() > 15

    return breakdown
```

### 2. 动态对冲比率

```python
def dynamic_hedge_ratio(price1, price2, window=60):
    """
    动态计算对冲比率
    """
    hedge_ratios = pd.Series(index=price1.index)

    for i in range(window, len(price1)):
        X = price2.iloc[i-window:i].values.reshape(-1, 1)
        y = price1.iloc[i-window:i].values

        model = LinearRegression().fit(X, y)
        hedge_ratios.iloc[i] = model.coef_[0]

    return hedge_ratios
```

---

## 下一步

- [均值回归策略](/kb/quant/quant-l6sjq7) - 深入学习均值回归
- [风险模型](/kb/quant/quant-s9qgsc) - 配对交易的风险管理
- [时间序列分析基础](/kb/quant/quant-bucbbs) - 统计基础
2:["$","$Lb",null,{"document":{"slug":"doc-8e2n0k","title":"配对交易策略实战","description":"","date":null,"tags":[],"category":"Quant","categoryId":"quant","content":"$c","filePath":"实战案例/配对交易策略实战.md","default":{"slug":"doc-8e2n0k","title":"配对交易策略实战","description":"","date":null,"tags":[],"category":"Quant","categoryId":"quant","content":"$d","filePath":"实战案例/配对交易策略实战.md"}}}]
a:[["$","meta","0",{"name":"viewport","content":"width=device-width, initial-scale=1"}],["$","meta","1",{"charSet":"utf-8"}],["$","title","2",{"children":"配对交易策略实战 - Quant | Jerry Xu"}],["$","meta","3",{"name":"description","content":"配对交易策略实战"}],["$","meta","4",{"name":"author","content":"Jerry Xu"}],["$","meta","5",{"name":"keywords","content":"Jerry Xu,徐劼瑞,上海交通大学,Sparkoh,AI Agent,Web,三维图形,个人网站,Next.js,TypeScript"}],["$","meta","6",{"property":"og:title","content":"Jerry Xu - 徐劼瑞"}],["$","meta","7",{"property":"og:description","content":"上海交通大学本科生，关注 AI Agent、Web 工具、三维图形与系统工程"}],["$","meta","8",{"property":"og:site_name","content":"Jerry Xu"}],["$","meta","9",{"property":"og:locale","content":"zh_CN"}],["$","meta","10",{"property":"og:type","content":"website"}],["$","meta","11",{"name":"twitter:card","content":"summary_large_image"}],["$","meta","12",{"name":"twitter:title","content":"Jerry Xu - 徐劼瑞"}],["$","meta","13",{"name":"twitter:description","content":"上海交通大学本科生，关注 AI Agent、Web 工具、三维图形与系统工程"}]]
1:null
